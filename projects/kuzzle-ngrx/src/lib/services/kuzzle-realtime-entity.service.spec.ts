import { TestBed } from '@angular/core/testing';

import { KuzzleRealtimeEntityService } from './kuzzle-realtime-entity.service';

describe('KuzzleRealtimeEntityService', () => {
  // //let service: KuzzleRealtimeEntityService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(KuzzleRealtimeEntityService);
  // });

  // it('should be created', () => {
  //   expect(service).toBeTruthy();
  // });
});
